# React LMS (Library Management System)

A lightweight React-based library management system starter built with Vite and Bootstrap. This project provides a simple admin dashboard and user-facing pages for common library workflows (login, signup, browsing books, borrowing records, profile management).

---

# Deploy Link

- Link: https://react-library-management-system-pr.vercel.app/

--- 

# Notes

- Admin Login:-
- Email - admin@gmail.com
- Password - admin123

- When use Userside or Adminside & any factionality run or changes then after  sometime refresh the page & updated changes show.

---

## 🚀 Quick Start

Prerequisites:
- Node.js 16+ and npm (or pnpm/yarn)

Install and run locally:

```bash
# install dependencies
npm install

# start development server
npm run dev
```

Open http://localhost:5173 (or the port printed by Vite) to view the app.

Build for production:

```bash
npm run build
npm run preview # to preview production build
```

---

## 🔧 Project Structure

Key files and folders:

- `index.html` — app entry HTML
- `src/` — React source files
  - `main.jsx` — app bootstrap
  - `App.jsx` — app routes and layout
  - `assets/` — images and static assets
  - `components/` — shared components
  - `admin/` — admin dashboard UI and pages
  - `userside/` — user-facing pages (Login, Signup, Home, MyBooks, etc.)
- `public/` — CSS, JS, third-party libs and assets used by the app
- `vite.config.js` — Vite configuration
- `package.json` — npm scripts and dependencies

---

## ✨ Features

- Vite + React (fast dev server and build)
- React Router for client-side routing
- Bootstrap for responsive UI
- Admin pages: Add/View Books, Dashboard
- User pages: Login/Signup, Home, MyBooks, Profile

---

## 🧩 Scripts

Defined in `package.json`:

- `npm run dev` — start Vite dev server
- `npm run build` — build production assets
- `npm run preview` — preview production build locally
- `npm run lint` — run ESLint

---

## ✅ Contributing

Contributions are welcome. Please:
1. Fork the repository
2. Create a feature branch
3. Open a PR with a clear description of your changes

---

## � Documentation

Short documentation has been added in the `docs/` folder:

- `docs/OVERVIEW.md` — project overview and quick start
- `docs/ARCHITECTURE.md` — architecture & stack
- `docs/ROUTES.md` — route list and mapping
- `docs/DEPLOYMENT.md` — build & deploy steps

---

## 📄 License

No license is included by default. If you'd like, I can add an `MIT` license or another license you prefer — tell me which one.

---
